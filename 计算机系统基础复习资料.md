**计算机系统基础复习资料**

**一、选择题（共10题）**

- 下列属于易失性存储器的是（  ）
        A. ROM  B. 闪存  C. DRAM  D. 光盘
- 直接寻址方式的特点是（  ）
        A. 形式地址即操作数  B. 形式地址即操作数地址  C. 形式地址即操作数地址的地址  D. 形式地址为操作数地址对应的寄存器编号
- cache直接相联映射方式的冲突率情况是（  ）
        A. 最低  B. 中等  C. 最高  D. 不确定
- 下列属于输出设备的是（  ）
        A. 鼠标  B. 扫描仪  C. 数/模转换器  D. 模/数转换器
- IEEE754浮点数中，阶码E的表示采用（  ）
        A. 原码  B. 补码  C. 移码  D. 反码
- 两正数相加结果为负，这种情况属于（  ）
        A. 正溢出  B. 负溢出  C. 无溢出  D. 不确定
- 追求用最少的指令完成复杂任务，指令系统庞大而复杂的是（  ）
        A. RISC  B. CISC  C. 两者都是  D. 两者都不是
- CPU中保存指令地址的是（  ）
        A. 累加器  B. 程序计数器PC  C. 地址寄存器  D. 数据寄存器
- 按内容进行访问的存储器是（  ）
        A. 随机存储器  B. 相联存储器  C. 只读存储器  D. 闪存
- 目的是扩充存储器容量的多体交叉存储器是（  ）
        A. 高位多体交叉  B. 低位多体交叉  C. 两者都是  D. 两者都不是

**二、填空题（共5题）**

- 指令功能条数=$2^m$，其中m为______的位数。
- 冯诺依曼体系结构的核心是______，即将解题步骤编制成程序，连同数据以二进制形式存放在存储器中。
- 大端方式是指存储器的低字节地址单元中存放的是数据的______字节。
- 海明码的校验位插入到信息位中对应的2⁰、2¹、2²、2³等______位置。
- 写回法的写操作特点是更新______即报告写完成。

**三、判断题（共10题）**

- 寄存器间接寻址中，形式地址为操作数的寄存器编号。（  ）
- 全相联映射的替换算法最简单。（  ）
- 补码乘法中，符号位需要单独处理，不与数值位一起运算。（  ）
- 0的原码和补码表示均有两种。（  ）
- DRAM的刷新按行进行，可减少存储矩阵的行数，增加列数从而减少刷新周期。（  ）
- 偶校验是指原始数据和校验码中1的总个数为奇数。（  ）
- 写穿法的写操作需要同时更新cache和主存才报告写完成。（  ）
- 汉字编码GB2312采用双字节编码，且每个字节的最高位MSB均为1。（  ）
- 低位多体交叉存储器的主要作用是扩充存储器容量。（  ）
- 相联存储器是按内容进行访问的存储器。（  ）

**四、简答题（共4题，每题5分，共20分）**

- 1. 简述系统互连中系统总线的定义及主要类型。
- 1. 简述奇偶校验的编码规则及局限性。
- 1. 简述指令格式的定义及指令格式需包含的核心问题。
- 1. 简述指令寻址方式的定义及分类。

**参考答案**

**一、选择题**

1. C  2. B  3. C  4. C  5. C  6. A  7. B  8. B  9. B  10. A

**二、填空题**

1. 操作码  2. 存储程序  3. 最高  4. 固定  5. cache

**三、判断题**

- ×（解析：寄存器间接寻址的形式地址为操作数地址对应的寄存器编号，寄存器寻址的形式地址才是操作数的寄存器编号）
- ×（解析：全相联映射的替换算法最复杂）
- ×（解析：补码乘法中符号位跟数值位一起运算）
- √
- √
- ×（解析：偶校验是指原始数据和校验码中1的总个数为偶数，奇校验才是总个数为奇数）
- √
- √
- ×（解析：低位多体交叉存储器的主要作用是提高访问速度，高位多体交叉的目的是扩充存储器容量）
- √

**四、简答题**

- 答：系统总线是CPU连接计算机中各主要部件的总线。总线是连接两个或多个设备的公共信息通路，主要有三种类型：①数据总线，用于传输数据信息；②地址总线，用于传输内存单元或I/O设备的地址信息；③控制总线，用于传输控制信号和时序信号，协调各部件的操作。
- 答：编码规则：在数据位后增加一位校验位，使校验码中1的个数为奇数（奇校验）或偶数（偶校验）。局限性：只能检测出奇数个位出错的情况，不能检测偶数个位出错。
- 答：指令格式是用二进制代码表示指令的结构形式。需包含的核心问题有三个：①指令要求计算机处理什么操作数？②指令要求计算机对操作数做什么操作？③计算机怎样才能得到操作数？
- 答：指令寻址方式是寻找指令或操作数有效地址的方式。主要分为两类：①指令寻址方式，包括顺序寻址、跳跃寻址；②操作数寻址方式，包括立即寻址、直接寻址、间接寻址、寄存器寻址、寄存器间接寻址、相对寻址、基址/变址寻址、复合寻址等。

|（注：文档部分内容可能由 AI 生成）
